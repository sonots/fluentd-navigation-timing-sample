<source>
  type tail
  path ../ltsvapp/log/application.log
  pos_file ../ltsvapp/log/application.log.pos
  format ltsv
  tag raw.navigation_timing
</source>

<match raw.navigation_timing>
  type navigation_timing
  tag navigation_timing
</match>

#<match navigation_timing>
#  type stats
#  interval 60s
#  add_tag_prefix stats
#  avg .*
#</match>

<match navigation_timing>
  type yohoushi
  mapping1 / http://localhost:5125
  key_pattern .* /hpbn/navigation_timing/${key}
</match>
